spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/n-enami/springapps-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
